/**
 *
 * - a region is an array of points
 *
 * @param {{ regions: [number, number][][], inverted: boolean }} polygon
 * @param {number} scale
 * @returns
 */
function renderPathsString(polygon, scale = 1) {
	// TODO: implement
	// var svgpath = "",
	// 	i,
	// 	j;
	// for (i = 0; i < paths.length; i++) {
	// 	for (j = 0; j < paths[i].length; j++) {
	// 		if (j === 0) {
	// 			svgpath += "M";
	// 		} else {
	// 			svgpath += "L";
	// 		}
	// 		svgpath += paths[i][j].X / scale + ", " + paths[i][j].Y / scale;
	// 	}
	// 	svgpath += "Z";
	// }
	// if (svgpath == "") svgpath = "M0,0";
	// return svgpath;
}
